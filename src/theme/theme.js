const breakpoints = ["40em", "52em", "64em"]

breakpoints.sm = breakpoints[0]
breakpoints.md = breakpoints[1]
breakpoints.lg = breakpoints[2]

const space = [0, 4, 8, 16, 32, 64, 128, 256, 512]

export default { breakpoints, space }
